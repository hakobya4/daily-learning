# Day 2, Task 4 -- Git: branch, commit, merge (do it, then explain it)

## THE TASK

In THIS repo (or a scratch repo if you'd rather not touch this one's
history), do the following for real, in your terminal, then fill in
the "what I ran" section below with the ACTUAL commands you used and
a one-sentence explanation of what each one did:

1. Create a new branch off `main`.
2. Make a small change and commit it on that branch.
3. Switch back to `main`.
4. Merge the branch into `main`.
5. Delete the now-merged branch.

## WHY THIS MATTERS

Branch -> commit -> merge -> delete is the core loop you'll use for
basically every feature/fix in a real job, including collaborating on
the ABAP/RAP portfolio project. The goal here isn't the git syntax
alone -- it's building the muscle memory for "isolate work on a
branch, integrate it back when it's ready, clean up after."

## WHAT I RAN (fill this in yourself -- don't skip it)

```
# TODO: paste your actual commands here, e.g.:
# git checkout -b my-branch-name
#   -> created and switched to a new branch called my-branch-name
# git add ... && git commit -m "..."
#   -> ...
# git checkout main
#   -> ...
# git merge my-branch-name
#   -> ...
# git branch -d my-branch-name
#   -> ...
```

## ONE THING THAT SURPRISED ME OR I HAD TO LOOK UP

TODO: write a sentence or two. (Forcing yourself to name a real
sticking point is more valuable than pretending it was all obvious.)
