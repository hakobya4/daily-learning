# Day 4 (2026-09-25) -- 10 tasks, one day

Posed by the daily automation per Narek's request: 10 unsolved
exercises a day, across rotating topics. Solve as many as you can;
nothing here is pre-solved.

1. **SQL** -- `topics/sql/day4-exercises.sql` (schema: `topics/sql/day4-schema.sql`)
   4 queries: RANK() by quantity within category, running total per
   category, LAG() quantity difference per product, top-2-per-category
   via ROW_NUMBER().
2. **SQL** -- `topics/sql/day4-correlated-subqueries.sql` (self-contained: schema + data included)
   3 queries: EXISTS for students in a CS course, NOT EXISTS for
   students with zero enrollments, correlated subquery for
   above-course-average grades.
3. **Python** -- `topics/python/run_length_encode.py`
   Run-length encode/decode a string.
4. **Python** -- `topics/python/valid_parentheses.py`
   Stack-based bracket-matching validity check.
5. **Unix/shell** -- `topics/unix/csv_column_sum.sh`
   Sum a numeric CSV column by column number, skipping the header.
6. **Unix/shell** -- `topics/unix/find_large_files.sh`
   Recursively find files above a size threshold, sorted largest-first.
7. **Git** -- `topics/git/day4-interactive-squash.md`
   Interactive rebase: squash a messy commit history, plus the
   commit --fixup / rebase --autosquash workflow.
8. **CS fundamentals** -- `topics/cs-fundamentals/binary_tree.py`
   Binary search tree: insert, contains, in-order traversal.
9. **ABAP/RAP** -- `topics/abap/day4-concept-notes.md`
   Explain a third RAP concept in your own words (different from Day 2's and Day 3's).
10. **Open source** -- `topics/opensource/day4-third-contribution.md`
    A third real-world issue/PR rep (Day 2's and Day 3's Task 10 are both still open too).

## Still open from previous days -- not forgotten, just old

These are listed here for visibility only; nothing below was touched
by today's automation run.

- `topics/cs-fundamentals/two_sum.py` (Day 2, Task 5) -- stub still
  raises `NotImplementedError`.
- `topics/cs-fundamentals/linked_list.py` (Day 2, Task 6) -- all four
  methods still raise `NotImplementedError`.
- `topics/abap/day2-concept-notes.md` (Day 2, Task 9) -- concept/notes
  sections are still unfilled `TODO`s.
- `topics/opensource/day2-first-contribution.md` (Day 2, Task 10) --
  "WHAT I DID" is still an unfilled `TODO`.
- `topics/opensource/day3-second-contribution.md` (Day 3, Task 10) --
  still explicitly "Not done yet" in the file itself.

Everything else from Day 2 and Day 3 (SQL, palindrome, word_frequency,
the git branch exercise, csv_stats, find_recent_matches, flatten_nested_list,
anagram_groups, log_level_summary, dedupe_lines, the Day 3 rebase
exercise, stack_queue, fibonacci_memo, and Day 3's ABAP notes) already
has real, working solutions in place -- only the five items above are
still open.

## Rule for all of today's 10

No solutions are provided. Run/test each one until it actually works,
then commit (and push, since that's your own GitHub auth) as you go.
